# MongoDB
MONGODB_URI=mongodb://localhost:27017
DATABASE_NAME=pnae_dev

# JWT Authentication
JWT_SECRET=your-secret-key-change-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440

# Storage Provider (mock for development)
STORAGE_PROVIDER=mock
MOCK_STORAGE_BASE_URL=http://localhost:8000/mock-storage

# LLM / AI Settings
# Para usar OpenAI, defina OPENAI_API_KEY e LLM_PROVIDER=openai
# Para desenvolvimento/testes, use LLM_PROVIDER=mock (padrão)
OPENAI_API_KEY=your-openai-api-key-here
LLM_PROVIDER=mock  # openai | mock
LLM_MODEL=gpt-4o-mini
RAG_EMBEDDING_MODEL=text-embedding-3-small
